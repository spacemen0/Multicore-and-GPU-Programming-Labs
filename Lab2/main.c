#include "stack.h"
#include <stdlib.h>
#include <cstdio>

int main() {
    stack_t *a ;
    a->head=(struct node*)malloc(sizeof(struct node*));
    a->head->value=3;
     //   printf("%d",a);
    // stack_push(a,2);

}